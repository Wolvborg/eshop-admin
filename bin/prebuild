#!/usr/bin/env node

/**
 * Module dependencies.
 */
var shell = require('shelljs');
var chalk = require('chalk');

/**
 * Shell Executions code
 */
shell.echo(chalk.blue('Deleting dist folder if exist'));
shell.rm('-rf', 'dist');
shell.echo(chalk.blue('Deleted dist folder'));
