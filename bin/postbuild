#!/usr/bin/env node

/**
 * Module dependencies.
 */
var shell = require('shelljs');

/**
 * Shell Executions code
 */
shell.echo('Moving the content from temp folder in dist folder');
shell.mv('temp/*', ' dist');

shell.echo('Deleting temp folder');
shell.rm('-rf', 'temp');
